# 📋 향후 고도화 개발 TODO List

이 문서는 프로젝트 포트폴리오 및 리소스 관리 시스템의 고도화를 위한 3대 핵심 옵션 기능의 세부 구현 계획과 진행 현황을 관리하는 할 일 목록입니다. 개발을 진행하면서 완료된 항목에 `[x]` 표시를 하여 진행 상태를 업데이트하십시오.

---

## 📈 [옵션 A] 실질 인건비 대비 수익률(Margin) 분석 기능

프로젝트 수주 금액(`price`)과 배정 인원들의 직급 급여 단가(`baseSalary`), 기여도(`contributionPercentage`)를 연산하여 실시간 마진율을 시각화합니다.

### 🛠️ 세부 할 일 목록

- [x] **백엔드 (DB & API) 개발**
  - [x] 프로젝트 조회 API 호출 시 `assignments.user.rank` 데이터를 모두 Join하여 깊은 포함(`include`) 형태로 반환하도록 Prisma 쿼리 보강.
  - [x] 프로젝트 시작일과 종료일 기간(개월 수)을 연산하고, 인원별 월 투입 인건비를 누적 합산하여 프로젝트의 `월간 투입 비용`, `예상 총 투입 비용`, `예상 마진액 및 마진율`을 산출하는 서버 측 헬퍼 함수 구현.
- [x] **프론트엔드 (타입 및 UI) 개발**
  - [x] `api.ts` 및 `UIProject` 인터페이스의 `financials` 속성에 마진 관련 필드(`monthlyCost`, `estimatedTotalCost`, `expectedMargin`, `marginPercent`, `salePrice`) 정의 추가.
  - [x] 프로젝트 상세 모달(`ProjectDetailModal.tsx`) 내에 비어있던 `Financial Health` 영역을 복구하여 **수주액 대비 누적 인건비 소진 게이지 바**와 **마진율 도넛 차트** 렌더링.
  - [x] 마진율이 10% 미만으로 도달할 경우 텍스트를 빨간색(Warning) 경고로 스위칭하는 조건부 렌더링 적용.

---

## 👥 [옵션 B] 중복 인력 할당 방지 및 스마트 인재 추천 기능

특정 기간 동안 직원의 총 투입 공수가 100%를 초과하는 중복 할당을 사전에 통제하고, 요구 기술 스택 기반 최적의 인재를 자동 정렬하여 추천합니다.

### 🛠️ 세부 할 일 목록

- [x] **백엔드 (중복 검증 및 추천) 개발**
  - [x] 전체 사용자 API 호출 시, 소속된 assignments와 project 정보를 함께 반환하여 중복 할당 여부 판별의 토대 구축.
  - [ ] 사원 ID와 투입 타겟 기간을 인자로 받아, 기존에 배정된 모든 프로젝트 기여도의 총합을 계산해 주는 가동률 검증 로직 구현.
  - [ ] 사원 배정 API(`POST /assignments`) 호출 시, 기여도 합계가 100%를 초과하게 될 경우 배정을 차단하고 에러(`400 Bad Request`)를 반환하는 백엔드 유효성 검증 적용.
  - [ ] 요구 기술 스택 ID 배열(`skillIds`)을 받아, 해당 기술 스택 일치율과 현재 가동률을 복합 산정해 최적의 추천 사원 순으로 정렬해 반환하는 `GET /users/recommend` 추천 API 구현.
- [/] **프론트엔드 (리소스 할당 UI) 개발**
  - [x] 다른 활성 프로젝트(Active, At Risk)에 배정된 인원에 대한 할당 토글 클릭 가드 및 카드 비활성화(`opacity-60 cursor-not-allowed`) 기능 구현.
  - [x] 배정 불가 사원에 대해 `배정 불가: [프로젝트명] 참여 중` 빨간색 경고 안내 뱃지 렌더링.
  - [ ] 프로젝트 생성 모달의 리소스 할당 단계(`NewProjectModal.tsx` 2단계) 상단에 **"스마트 추천 인재"** 섹션을 추가하여 추천 순으로 사원 카드 우선 정렬.
  - [ ] 각 사원 카드 내에 현재 전사 투입 가동률을 백분율(`가동률: N%`)로 실시간 노출.
  - [ ] 이번 프로젝트 배정으로 총합이 100%를 초과할 경우, 카드의 테두리를 빨간색 경고선으로 변경하고 할당 토글 버튼을 비활성화하는 UX 가드 추가.

---

## ⚙️ [옵션 C] 사용자 권한 제어 (RBAC) 설계

사용자 직급 및 시스템 역할(`ADMIN`, `PM`, `MEMBER`)을 기반으로 화면의 메뉴 노출 범위와 수정/삭제 기능 권한 및 API 엔드포인트 접근을 물리적으로 제한합니다.

### 🛠️ 세부 할 일 목록

- [ ] **데이터베이스 및 백엔드 보안 구축**
  - [ ] `schema.prisma` 내 `User` 모델에 `role String @default("MEMBER")` 필드 추가 및 Enum 반영.
  - [ ] `seed.ts` 파일 내 기본 사용자 데이터들에 `ADMIN`, `PM`, `MEMBER` 권한을 차등 할당하고 `npx prisma db push --accept-data-loss`로 데이터베이스 갱신.
  - [ ] 백엔드 라우터에 JWT 토큰 내의 `role` 필드를 파싱하고 유효한지 검증하는 인가 미들웨어(`checkRole`) 구현.
  - [ ] `/projects` (POST/PUT/DELETE) 및 `/settings` API 경로에 `checkRole(['ADMIN', 'PM'])` 미들웨어를 장착하여 권한 없는 API 호출 즉각 차단.
- [ ] **프론트엔드 권한 분기 및 접근 제어**
  - [ ] 로그인 정보를 전역 저장하고 로그인된 사용자의 권한을 제공하는 `authStore.ts` 추가.
  - [ ] 사이드바 메뉴 렌더링 시, `MEMBER` 권한을 가진 사용자는 `Settings` 및 `IT Assets` 메뉴가 노출되지 않도록 필터링.
  - [ ] 프로젝트 포트폴리오 리스트 및 모달 창에서 사용자가 `MEMBER`일 경우 '프로젝트 만들기(NewProjectCard)' 및 상세 모달 내 '프로젝트 수정(EditProjectModal)' 버튼 숨김 처리.
